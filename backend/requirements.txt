# --- Runtime ---
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic-settings==2.3.4
SQLAlchemy[asyncio]==2.0.31
asyncpg==0.29.0          # async (app_rw) baglanti surucusu
psycopg[binary]==3.2.1   # sync — alembic migration + testler (psql binary'sine gerek yok)
alembic==1.13.2          # /contracts/db migration'larini UYGULAR (yeni migration uretmez)
redis==5.0.7
celery==5.4.0

# --- Kripto (NTAG424 SDM/SUN dogrulamasi: AES-CBC/CMAC/GCM) ---
cryptography==42.0.8

# --- Auth ---
PyJWT==2.8.0
bcrypt==4.1.3
email-validator==2.2.0    # pydantic EmailStr icin

# --- Storage (MinIO / S3-uyumlu) ---
boto3==1.34.131           # presigned URL uretimi (yerel imzalama)

# --- Test ---
pytest==8.2.2
pytest-asyncio==0.23.7
httpx==0.27.0
