# Backend API base URL — /v0 ONEKI YOK (bkz. /contracts/openapi.yaml servers).
# Sunucu tarafi (BFF route handler'lari) bu degeri kullanir; istemci backend'e
# DOGRUDAN gitmez (token httpOnly cookie'de, asla JS'e acilmaz).
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# Uretimde: https + gercek domain
# NEXT_PUBLIC_API_BASE_URL=https://api.example.com
